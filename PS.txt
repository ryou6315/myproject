パスキー非対応のブラウザでは、Passkey（WebAuthn）による認証自体を実行することはできません。

その場合、Passkey が自動的に別の認証方式へ変換されるのではなく、Okta の認証ポリシーで許可されている他の Authenticator（例：Password、Okta Verify など）へフォールバックします。

なお、Passkey のみを必須の認証方式として設定している場合は、フォールバックは行われず、認証は失敗します。
