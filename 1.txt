import subprocess

def trace_route(domain):
    try:
        result = subprocess.run(["tracert", domain], capture_output=True, text=True, timeout=30)
        return result.stdout
    except Exception as e:
        return f"tracert failed: {e}"

trace_result = trace_route("outlook.office.com")
print(trace_result)
